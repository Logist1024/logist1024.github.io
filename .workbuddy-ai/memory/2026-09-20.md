# 2026-09-20

## 主页项目内容同步 GitHub
- 任务：把 `src/views/Home.vue` 的 `projects` 数组与 GitHub 用户 Logist1024 的公开仓库对齐。
- 结果：GitHub API 仅返回 2 个公开仓库 `logist1024.github.io`、`ServerTicket`；`Pages-docs` 已 404（删除/转移/改名），从网页移除。
- ServerTicket 的 tags 已同步为 GitHub topics：`java, minecraft, minecraft-plugin, paper, spigot, ticket-system`（去掉了非 topic 的 jakarta-mail）。
- 主页自身与 ServerTicket 的「简介」保留手写中文描述（GitHub 上 description 为空/英文，且站点为中文），未用官方英文描述覆盖。
- 验证：`npm install`（node_modules 原本为空）+ `npm run build` 通过；grep dist 确认 Pages-docs 已消失、两个现存仓库存在。
- 注意：`node_modules` 此前未安装，构建前需先 `npm install`。
- 部署：`.github/workflows/deploy.yml` 在 push 到 main 时自动 `npm ci` + `npm run build` 并发布到 GitHub Pages。
- 提交：已在本地 main 创建 commit（仅改动 Home.vue）。但本沙箱无 GitHub 凭据（credential.helper=helper-selector 无缓存、无 GITHUB_TOKEN），`git push` 无法认证；需用户在本机终端 `git push origin main` 完成发布。
